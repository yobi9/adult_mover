# Architecture - Adult Media Mover

ناقل الأفلام والمسلسلات للكبار فقط (Adult Media Mover)

---

## 1. ملخص المتطلبات المحلّلة

تطبيق سطح مكتب يعمل على Windows. وظيفته الأساسية:

1. قبول عدة **مصادر** (مجلدات محلية، أقراص، مجلدات شبكة، مسارات UNC).
2. فحص المجلدات الفرعية داخل المصادر وتحديد ما يحتوي على ملفات فيديو.
3. استخراج الاسم الحقيقي للمحتوى من اسم المجلد «الخام» (Thread Title Parsing).
4. الاستعلام عن TMDB API عبر REST للحصول على: النوع (Movie / TV)، والتصنيف العمري.
5. نقل **المجلد بالكامل** إلى مجلد الوجهة فقط عندما يكون التصنيف ضمن قائمة التصنيفات المستهدفة للكبار.

### النموذج الأمني: Fail Closed

الافتراض الافتراضي في كل نقطة عدم يقين هو **عدم النقل**:

| الحالة | القرار |
|---|---|
| لم يُعثر على المحتوى في TMDB | SKIP |
| لا يوجد تصنيف عمري | SKIP |
| فشل الاتصال / خطأ قراءة / خطأ شبكة | SKIP |
| نتيجة البحث منخفضة الثقة | SKIP |
| أي خطأ غير متوقع | SKIP |

البرنامج **Mover وليس Deleter**: لا حذف، لا استبدال، لا تجاوز صلاحيات.

---

## 2. التقنية المختارة

### الاختيار: Node.js + Electron + TypeScript

| الطبقة | التقنية |
|---|---|
| لغة البرمجة | TypeScript 5.9 (تُجمَّع إلى JavaScript) |
| بيئة التشغيل | Node.js 24 (مؤكد توفرها في بيئة العمل) |
| هيكل سطح المكتب | Electron 44 (Chromium + Node في عملية واحدة) |
| الواجهة | HTML/CSS/JavaScript خام (Renderer) بدون Framework ثقيل |
| الاختبارات | node:test المدمج + tsx لتشغيل TypeScript |
| البناء/Tوزيع | electron-builder 26 (NSIS Installer + Portable EXE) |
| HTTP | fetch المدمج (Node 24) |

### لماذا هذه التقنية؟

1. **دعم Windows وUNC**: `fs` و `path.win32` في Node تدعم مسارات `\\Server\Share` و `\\192.168.1.10\Media` أصلاً دون افتراض حرف Drive، تماماً كما يتطلب البند 32.
2. **عدم تجمد الواجهة**: كل I/O في Node غير حاجب (Async – libuv)، والواجهة تعيش في عملية Chromium منفصلة، لذلك تبقى قابلة للتحريك والتحديث أثناء الفحص والنقل (البند 6 و 37).
3. **Unicode والعربية**: Node/Chromium UTF-8 أصلاً؛ تسمية الملفات والمجلدات العربية تعمل مباشرة (البند 33).
4. **الواجهة العربية RTL**: Chromium يمنح تحكماً كاملاً في الاتجاه `dir="rtl"` والخطوط والتصميم المعاصر دون قيود أدوات واجهة قديمة.
5. **REST API سهل**: `fetch` المدمج مع معالجة Retry/Backoff مكتوبة فوقه.
6. **توزيع المستخدم النهائي**: electron-builder ينتج Installer/Portable EXE؛ المستخدم لا يحتاج أدوات تطوير.
7. **الصيانة**: TypeScript صارم + وحدات Core خالصة من Electron (قابلة للاختبار بدون تشغيل GUI) + اختبارات مدمجة بلا تبعيات خارجية.
8. **استهلاك الموارد**: لا Framework واجهة ثقيل؛ العمل الثقيل في Main Process فقط؛ Renderer خفيف.

### البدائل التي فُكر فيها ولماذا رُفضت

| البديل | السبب |
|---|---|
| **Python + PyInstaller + Tkinter/Flet** | غير مثبت في بيئة العمل (كامل السلسلة مفقود). لو كان متاحاً فهو بديل قوي، لكن أدوات Tkinter الضعيفة للعربية/RTL وغياب التوكن بمثابة مخاطرة، إلى جانب الحاجة الكاملة لتثبيت بيئة جديدة. |
| **C# WPF / .NET** | خيار ممتاز نظرياً لسطح المكتب Windows الأصيل، لكن أداة .NET غير مثبتة أصلاً في بيئة العمل، وسلسلة بناء msbuild أثقل، والمرونة البصرية للعربية/RTL أقل من Chromium. |
| **Tauri (Rust)** | ادعى أفضل استهلاك ذاكرة، لكنه يتطلب Rust Toolchain كاملاً غير موجود، ودورة تطوير أبطأ مع بنية أصعب للعمليات الطويلة على UNC. |
| **Go + Fyne/Wails** | الأداة غير متوفرة، ونضج بيئة الواجهات أقل، ونظام النوافذ أقل احترافية. |
| **Electron فقط دون TypeScript** | تم رفضه: TypeScript يضيف أمان الأنواع لقرارات البحث والنقل الحرجة ويخدم البند 45 (جودة الكود) و 44 (التوثيق). |

### ملاحظة عملية

Electron أثقل من Tauri للذاكرة، لكن البيئة المتاحة (Node فقط) تجعله الخيار الواقعي الوحيد ذا النضج الكامل، مع تخفيف الأثر عبر واجهة خام خفيفة وجميع العمل في Main Process.

---

## 3. بنية المشروع

```
adult_mover/
│
├── source/                      # الكود المصدري
│   ├── main/                    # عملية Electron الرئيسية (Main Process)
│   │   ├── main.ts              # إنشاء النافذة، دورة حياة التطبيق
│   │   ├── preload.ts           # جسر IPC آمن (contextBridge)
│   │   └── ipc.ts               # تسجيل قنوات IPC
│   │
│   ├── core/                    # منطق خالص (لا يعتمد على Electron = قابل للاختبار)
│   │   ├── media-parser/        # استخراج الاسم/السنة/النوع من اسم المجلد
│   │   │   ├── parser.ts
│   │   │   └── tokens.ts        # قواميس العناصر التقنية (1080p, WEB-DL, HEVC...)
│   │   ├── ratings/             # تطبيع التصنيفات + التصنيفات المستهدفة
│   │   │   ├── normalization.ts
│   │   │   └── targets.ts
│   │   ├── tmdb/                # عميل TMDB API
│   │   │   ├── client.ts        # REST + Retry/Backoff + تصنيف الأخطاء
│   │   │   ├── search.ts        # TV-first ثم Movie + اختيار النتيجة
│   │   │   ├── ranking.ts       # درجة الثقة (الاسم/السنة/التشابه/الترتيب)
│   │   │   └── media-rating.ts  # getMediaRating المركزية (المكافئ لـ get_media_rating)
│   │   ├── files/               # إدارة الملفات والمجلدات
│   │   │   ├── types.ts         # امتدادات الفيديو + أدوات فحص
│   │   │   ├── path-guard.ts    # حماية الوجهة/المصدر + كشف التداخل + UNC
│   │   │   ├── mover.ts         # النقل الآمن + التسمية الفريدة + الفحوصات قبل النقل
│   │   │   └── scanner.ts       # فحص المجلدات (متكرر أو لا) + تحديد مجلد المحتوى
│   │   ├── pipeline/            # التنسيق: فحص → تحليل → TMDB → قرار → نقل
│   │   │   ├── orchestrator.ts  # حلقة المعالجة المتسلسلة لكل مجلد
│   │   │   ├── decision.ts      # أنواع القرار MOVE|SKIP|ERROR + الأسباب
│   │   │   ├── stop.ts          # آلية الإيقاف الآمن
│   │   │   └── stats.ts         # الإحصائيات
│   │   ├── config/              # حفظ الإعدادات غير الحساسة (اختياري)
│   │   │   └── store.ts
│   │   ├── logging/             # السجل: حلقة داخلية في الذاكرة + ملف اختياري
│   │   │   └── logger.ts
│   │   └── types.ts             # أنواع مشتركة
│   │
│   └── renderer/                # الواجهة (عملية Chromium)
│       ├── index.html
│       ├── styles.css
│       ├── renderer.ts          # منطق الواجهة
│       └── i18n.ts              # نصوص عربية/إنجليزية
│
├── tests/                       # اختبارات node:test (تعمل دون Electron)
│   ├── media-parser.test.ts
│   ├── ratings.test.ts
│   ├── tmdb.test.ts             # Mocks فقط، لا اتصال حقيقي
│   ├── ranking.test.ts
│   ├── path-guard.test.ts
│   ├── mover.test.ts
│   ├── scanner.test.ts
│   ├── pipeline.test.ts         # TMDB Stub + مجلدات مؤقتة
│   └── fixtures/                # ملفات فيديو تجريبية فارغة صغيرة
│
├── assets/                      # أيقونات البناء (icon.ico / icon.png)
├── scripts/
│   └── copy-assets.mjs          # نسخ renderer + assets إلى dist قبل البناء
│
├── test-data/                   # (مُستثنى من git) مجلدات تجريبية مؤقتة
├── dist/                        # (مُستثنى من git) مخرجات البناء
│
├── package.json
├── tsconfig.json                # إعداد التطبيق (source → dist)
├── tsconfig.test.json           # typecheck للاختبارات أيضاً
├── electron-builder.yml         # إعداد Installer/Portable
├── .gitignore
├── README.md                    # يُكتب في مرحلة لاحقة
├── Architecture.md
├── ImplementationPlan.md
└── Requirements.md
```

---

## 4. مكونات النظام

| المكوّن | المسؤولية |
|---|---|
| **MediaParser** | تحويل `Breaking.Bad.S05.1080p.WEB-DL` إلى `Breaking Bad` مع استخراج `S01`/`S01E01` والسنة والدقة والنوع المتوقع (TV hint إذا وُجد Season/Episode). |
| **Ratings** | تطبيع التصنيف (`tv-ma`/`TV-MA`/` TV-MA` → قيمة واحدة) + قائمة الهدف `TV-MA, R, NC-17, X, 18+, ADULT, 18` + قرار `isTargeted`. |
| **TMDBClient** | REST خام مع Limiter عالمي، Retry/Backoff، تصنيف أخطاء، إخفاء المفتاح في الأخطاء. |
| **SearchRanking** | حساب درجة الثقة بين الاسم المطلوب ونتائج TMDB (تطابق الاسم، السنة، التشابه النصي، ترتيب TMDB). |
| **MediaRating** (المركزية) | `getMediaRating(name, year, apiKey)` → `{ rating, mediaType, tmdbId, confidence, state }`. **TV أولاً ثم Movie** بحسب البند 17-19. |
| **FileManager** | Scanner (اكتشاف مجلد المحتوى) + PathGuard (حماية التداخل) + Mover (نقل آمن، تسمية فريدة، فحوصات قبل النقل؛ Same-Volume تلقائي، Cross-Volume مُرفض بلا تأكيد صريح). |
| **Orchestrator** | حلقة متسلسلة لكل مجلد؛ يدير Stop/Stats، ويستدعي المراحل ويحوّل القرارات إلى Log. |
| **StopController** | طلب الإيقاف → أكمل المجلد الحالي → ألغِ الجدولة بعدها. |
| **ConfigStore** | حفظ اختياري للإعدادات غير الحساسة (آخر المصادر، الوجهة، تفضيلات الواجهة). المفتاح لا يُحفظ إلا بموافقة صريحة (خانة اختيار). |
| **Logger** | حلقة دائرية في الذاكرة للعرض الفوري + ملف اختياري (متناوب)؛ مفتاح API ممنوع من كل أنواع التسجيل. |
| **UI (Renderer)** | العرض والإدخال فقط؛ لا منطق ملفات أو شبكة أبداً. |

---

## 5. طريقة عمل الواجهة (UI Architecture)

- **فصل صارم**: Renderer → (IPC عبر contextBridge) → Main Process → Core.
- Renderer يملك فقط: قائمة المصادر، حقل الوجهة، حقل المفتاح (مُخفى `type=password`)، أزرار (إضافة/حذف المحدد/حذف الكل/استعراض/بدء/إيقاف)، حالة التشغيل، إحصائيات، Log فوري، وخانة «فحص متكرر».
- Main Process يرسل أحداثاً دفعية (push) للـ Renderer: `log`, `status`, `stats`, `jobDone`, `jobAborted`, `summary`. الـ Renderer يحدّث فقط. هذا يضمن عدم التجميد.
- اتجاه RTL: `dir="rtl" lang="ar"` للجذر، نصوص عربية أساساً مع أخطاء/مصطلحات تقنية بالإنجليزية عند الحاجة.
- **حالة التشغيل** (Status Machine): جاهز ← جاري التحضير ← جاري فحص المصادر ← جاري تحليل الاسم ← جاري البحث في TMDB ← جاري النقل ← جارٍ الإيقاف ← تم الإيقاف ← اكتملت العملية.
- **إغلاق أثناء التشغيل** (البند 37): حوار تحذيري «هناك عملية قيد التشغيل… هل تريد الإيقاف الآمن ثم الإغلاق؟» نعم → Stop ثم انتظار Worker عبر Promise دون تجميد ثم إغلاق. لا → يبقى مفتوحاً.

---

## 6. طريقة التعامل مع TMDB API

- البنية الأساسية: `https://api.themoviedb.org/3` مع `api_key` في query.
- النقاط المستخدمة:

| النقطة | الغرض |
|---|---|
| `GET /search/tv?query=...&year=...&language=...` | البحث الأول عن مسلسل (البند 17) |
| `GET /tv/{id}/content_ratings` | تصنيف المسلسل: أولوية `US` ثم بديل مسجل في Log (البند 18) |
| `GET /search/movie?query=...&year=...` | البحث عن فيلم إذا لم يُعثر على مسلسل مناسب (البند 19) |
| `GET /movie/{id}/release_dates` | تصنيف الفيلم: `US` ثم `certification` (البند 20) |

- **التالي: TV أولاً ثم Movie** حرفياً كما يطلب البندان 17-19. التصنيف من نقطة التفاصيل فقط.
- **Limiter عالمي**: طلب واحد كل ≥ 400ms (≈2.5 req/s) عبر كل الجدولة — يمنع حدوث 429.
- **Retry محدود مع Backoff**: على 429/6xx/Timeout → `1s, 2s, 4s` ثم توقف عن المجلد (SKIP). لا Loop لا نهائي (البند 25).
- **معالجة منفصلة للأخطاء** (البند 26):

| الكود | السلوك |
|---|---|
| `401` / `403` | مفتاح غير صالح/غير مصرح → تسجيل، إعلام المستخدم، **إيقاف الوظيفة كاملة** (لا متابعة بلا فائدة) |
| `404` | محتوى غير موجود في نقطة التفاصيل → SKIP |
| `429` | Backoff المحدود أعلاه ثم SKIP |
| `500/502/503` | Retry محدود ثم SKIP |
| Timeout / Connection | تصنيف كخطأ شبكة؛ Retry محدود ثم SKIP |

- **التحقق من الاستجابة**: بنية JSON تُفحص قبل الاستخدام (Fail Closed).
- `getMediaRating` ترجع `{ rating, mediaType: 'series'\|'movie'\|'unknown', tmdbId, confidence, state }`.

---

## 7. طريقة إدارة العمليات الخلفية

- كل العمل الثقيل في **Main Process** (Node) وهو I/O غير حاجب أصلاً: `await` على جميع عمليات fs والشبكة — **لا شيء يجمّد النافذة**.
- **Renderer منفصل** (Chromium) عن ذلك تماماً، لذلك يبقى التحرك/التحديث مرناً دائماً.
- معالجة المجلدات **متسلسلة لا متوازية** (قرار هندسي): ترتيب واضح للـ Log، ضغط على Limiter TMDB، سلوك توقع أسهل، وإيقاف آمن أبسط. الاتصالات الفردية داخل نقلة الملف الكبيرة غير الحاجبة لا تمنع الاستجابة.
- أحداث التقدم تُدفع للواجهة عبر `webContents.send`.
- تكرارات كبيرة الملفات: النسخ عبر Streams مع تحديث تقدم غير حاجب.

---

## 8. آلية الإيقاف الآمن

البند 7: «الإيقاف لا يعنو قتل العملية».

- `StopController` يحمل منطقة إشارة ذرية (Atomic Flag).
- تُفحص العلامة (أ) *بين المجلدات* و(ب) *بين الملفات داخل نقلة مجلد واحدة*.
- لا يتم قطع **نقل جارٍ أبداً**: يُكمل المجلد الحالي حتى تتحقق نسخة كاملة.
- بعد اكتمال المجلد الحالي: لا جدولة لمجلدات جديدة ← إعداد «تم الإيقاف» + ملخص.
- لا `process.kill` ولا `app.exit` قسري.
- **إغلاق النوافذ أثناء التشغيل**: الحوار التحذيري في قسم الواجهة (5/37).

---

## 9. طريقة التعامل مع الملفات

- **لا حذف كجزء من سير العمل**: لا عمليات حذف تلقائية إطلاقاً.
- **نقل = Mover وليس Deleter**:
  - **نفس القرص/Volume (Same Volume)**: `fs.rename` للمجلد — شبه ذري وسريع، ينفَّذ تلقائياً.
  - **عبر أقراص (Cross Volume)**: **لا تنفيذ تلقائي**. العملية تتطلب `Copy → Verify → Delete Source`، وقاعدة الإزالة التلقائية للمصدر **محظورة افتراضياً**:
    - تُعتبر غير قابلة للنقل تلقائياً.
    - يُسجل السبب في Log («النقل بين Volumes يتطلب تأكيداً صريحاً»).
    - يُعرض للمستخدم أن النقل بين Volumes يحتاج إلى تأكيد/إعداد صريح.
    - **لا يُحذف المصدر أبداً إلا عبر خيار إضافي واضح ومقصود من المستخدم يسمح بذلك، وهذا الخيار غير مفعّل افتراضياً** (لا يوجد في هذه النسخة).
- **التسمية الفريدة**: إن وُجد `Movie A` في الوجهة → `Movie A_1`, `Movie A_2` … (البند 11).
- **الفحوصات قبل النقل** (البند 36): المصدر موجود/مجلد صالح، الوجهة متاحة، الوجهة ليست داخل المصدر بالطريقة الخاطئة، الاسم غير مستخدم (بعد التسمية الفريدة)، المحتوى ما زال موجوداً، القرار ما زال صالحاً.
- **امتدادات الفيديو** (غير حساسة لحالة الأحرف): `mp4, mkv, avi, mov, wmv, flv, webm`.
- **مجلة المحتوى** = مجلد يحتوي ≥ فيديو واحد مباشرة. عند اكتشافه يُنقل بالكامل (بما فيه مجلداته الفرعية) ولا يتم الغوص داخله (البند 9).
- **الفحص المتكرر (Recursive)**: خانة في الواجهة، الافتراضي On، واضحة الملصق (البند 9).
- **ملفات قيد الاستخدام / غير قابلة للنقل**: لا حذف، سجل الخطأ، انتقل للمجلد التالي بأمان (البند 34).
- كل المسارات تتعامل كسلاسل نصوص، وتعالج عبر `path.win32` حتى تبقى UNC صالحة (البند 32).

---

## 10. دعم UNC

- الاعتماد على `path.win32` في **كل** التعامل مع المسارات؛ لا افتراض لوجود حرف Drive.
- `\\Server\Share`، `\\NAS\Movies`، `\\192.168.1.10\Media` تُعالج كمجلدات عادية.
- **PathGuard** يقارن المسارات بشكل ثابت مع تطبيع (حذف الأشرطة الزائدة، توحيد حالة الأحرف) ليكتشف:
  - الوجهة نفسها كمصدر.
  - المصدر داخل الوجهة **أو** الوجهة داخل المصدر (البند 31).
- أخطاء الشبكة/الصلاحيات/غير المتاح/التباطؤ/الملفات قيد الاستخدام تُصنف وتُسجل ويمر البرنامج للمجلد التالي دون انهيار (البند 32).

---

## 11. استراتيجية معالجة الأخطاء

- **مبدأ عام**: كل معالج يسمى «Fail Closed» عند الشك.
- **تصنيف الأخطاء**: `AuthError` (401/403) | `RateLimit` (429) | `NotFound` (404) | `ServerError` (5xx) | `NetworkError` (timeout/connection) | `FileSystemError` | `ValidationError`.
- **عنصر القرار** في الحلقة يختم الوضع النهائي لكل مجلد: `MOVE | SKIP | ERROR` مع `reason` مبنية.
- **حدود الأمان**: try/catch حول كل مرحلة pipeline؛ خطأ مجلد واحد لا يوقف البرنامج (البند 50). الاستثناء الوحيد: AuthError يوقف الوظيفة كاملة مع إعلام المستخدم.
- **سجل الأخطاء** في واجهة رؤية المستخدم يحوي السبب والفعل (مثل `Action: SKIP`) دون Stack Trace؛ وضع **Debug** خلف flag تطويري يعرض التفاصيل الفنية.
- **أسماء/مفاتيح**: مفتاح API لا يظهر في الواجهة بعد الإدخال، ولا في أي رسالة Log أو خطأ.

---

## 12. استراتيجية الاختبارات

الاختبارات تعمل **بلا Electron** (core خالص) عبر `node --import tsx --test`:

| التغطية | الطريقة |
|---|---|
| استخراج الاسم والسنة وتنظيف العناصر التقنية | MediaParser + حالات متعددة شاملة |
| تطبيع التصنيف | Ratings.normalize |
| تحديد التصنيف المستهدف | Ratings.isTargeted |
| اختيار نتيجة TMDB | Ranking مع بيانات Fake (لا شبكة) |
| التسمية الفريدة | Mover vs مجلدات مؤقتة |
| منع نقل الوجهة إلى نفسها / التداخل | PathGuard + مسارات UNC وهمية |
| المسارات غير الصالحة | Phantom paths + أخطاء متوقعة |
| الحلقة الكاملة مع TMDB Stub | Pipeline على مجلدات مؤقتة (Movie A, Movie B, Series A, Series B, Not Media، عربي، موجود سابقاً في الوجهة، فشل نقل محاكى) |
| Logging (لا تسريب مفتاح) | Logger + assert عدم ظهور المفتاح |

- **لا تعتمد اختبارات TMDB على الإنترنت إطلاقاً**: `fetch` يُستبدل بـ Mock/Stub (البند 40).
- **لا لمس ملفات المستخدم**: كل اختبارات الملفات على `mkdtemp` مؤقتة.
- **اختبار حقيقي ميداني** (البند 41): script يجرد مجلد `test-data/` بملفات فيديو فارغة/صغيرة ويشغّل Pipeline كاملاً بنمط Sandbox (الوجهة مجلد مؤقت أُعدّ له) — لا أي تأثير على بيانات حقيقية.

---

## 13. بناء نسخة Windows النهائية

- `npm run build` → تجميع TypeScript إلى `dist/` ثم نسخ `renderer` + `assets`.
- `npm run dist` → `electron-builder --win nsis portable`:

  | الناتج | الوصف |
  |---|---|
  | `NSIS Installer (.exe)` | مثبت كلاسيكي (اختصارات، إلغاء تثبيت). |
  | `Portable (.exe)` | نسخة محمولة بتشغيل مباشر. |

- userData/التكوين تحت `%APPDATA%\AdultMediaMover`.
- المستخدم النهائي **لا يحتاج أدوات تطوير**.
- التوقيع الرقمي (Code Signing) **اختياري**: بدون شهادة سيظهر SmartScreen يُوضَّح في README.
- الأيقونة من `assets/`.

---

## 14. قرارات هندسية للنقاط الغامضة/الناقصة

1. **الترتيب TV ثم Movie**: يُنفَّذ حرفياً كما في البند 17-19 رغم كونه أبطأ للملفات الواضحة؛ التصنيف في النهاية من نقطة التفاصيل فقط.
2. **المعالجة متسلسلة لا متوازية**: أمان للترتيب والـ Rate Limit وسهولة التوقع، والصبر مردود على الواجهة عبر IPC.
3. **نقل عبر أقراص محظور افتراضياً**: `Copy → Verify → Delete Source` يُعتبر إزالة تلقائية للمصدر وهي **محظورة افتراضياً**. Same-Volume يُنقل عبر `fs.rename`. Cross-Volume يُسجل سببه ويُعرض للمستخدم رفضه كوضع غير قابل للنقل التلقائي؛ لن يُحذف المصدر إلا بخيار صريح وقصدي من المستخدم (غير مفعّل افتراضياً) — لا يوجد في هذه النسخة.
4. **الفحص المتكرر الافتراضي On** مع إمكانية إيقافه من الواجهة؛ «مجلد المحتوى» هو درجة أول يحتوي فيديو مباشرةً ويُغلب عليه النقل الكامل.
5. **مفتاح API**: في الذاكرة فقط افتراضياً؛ الحفظ الدائم **اختيار صريح** (خانة «حفظ المفتاح محلياً») في JSON محمي داخل AppData مع تحذير واضح. لا يُسجل في Log/ملفات أبداً ما لم يُطلب صراحةً.
6. **حفظ الإعدادات (غير الحساسة)**: المصادر الأخيرة، الوجهة، تفضيلات الواجهة تُحفظ اختيارياً في `%APPDATA%` (البند 38).
7. **المصدر يحتوي فيديو مباشرةً**: يُعامل المصدر فقط كمرشّح محتوى عند تطبيق الفحص المتكرر (تغطية حالة طرح المصدر نفسه كمجلد محتوى).
8. **أولية إزالة بقايا نسخ مؤقتة**: النسخ المقاطَعة تبقى بأسماء `.tmp-moving-*` معروفة؛ يُسجل وجودها دون حذفها صامتاً (يقف عند Log).
9. **مجلدَتي الوجهة والمصدر على UNC**: الوجهة أيضاً قد تكون UNC وتُكتب تلقائياً إذا كانت صالحة (البند 2 / 4 - `استعراض`).
10. **استهلاك الموارد**: كل العمل الثقيل في Main Process؛ Renderer خفيف ولا يجرى أي فحص فيه.

---

## 15. تصنيف الماسح الضوئي (Scanner) — Media Root / Series Root / Season Container

يُبنى التصنيف على `parseMediaName` نفسه (لا تُضاف قائمة أسماء مواسم مكتوبة يدوياً)،
ويعمل على مستويين:

1. **مجلد محتوى (Media Root):** مجلد يحتوي ملف فيديو مباشرةً — يُعاد لقاعدة
   المسؤوليات (قرار TMDB ثم نقل المجلد كاملاً). هذا التصنيف يعلو أي شيء آخر.
2. **حاوية مواسم (Season Container):** مجلد محلِّله الدولي يعيده بدون عنوان
   (كما في `الموسم 01`, `S01`, `Season 1`, `S01E01` إلخ). هذه الحاويات لا تُعالج
   كمرشّحين مستقلين: لا تُستعلم TMDB لكل موسم على حدة.
3. **جذر سلسلة (Series Root):** مجلد (بشرط عمقه ≥ 1) كل أطفاله المباشرون
   (بأسمائهم فقط) حاويات مواسم، وليس فيه فيديو مباشر. يُعالج كمرشّح واحد
   ويُنقل بالهرمية كاملةً.

### السلوك التفصيلي
- عند تفعيل الفحص المتكرر (Recursive) تُكتشف هذه الحاويات وتُكتمل،
  وعند إيقافه يُفحص المستوى الأول فقط مع بقاء التمييز أعلاه.
- مصدر يُشير إلى مجلد متصل مباشرةً بفيديو يبقى مجرد مرشّح محتوى — لا يُرفع
  لمستوى «جذر سلسلة». (حماية تقصّدية: لا يُرفع «مكتبة كاملة» كسلسلة.)
- مجلد محتوى غائر (لا فيديو مباشر) لا يُرفع ولا يُعالج — بسبب الحفاظ على العمق.
- لا يُنزل داخل حاويات المواسم الأخرى بحد ذاتها (يُنزل داخل مجلدات المحتوى فقط).
- النتائج (ON/OFF) موثقة في اختبارات `tests/scanner.test.ts` وبحث TMDB حقيقي
  في السيناريوهات المعبأة (4 مرشّحين ON / 3 OFF لبنية نموذجية مع سلسلتين
  ومواسمهما).