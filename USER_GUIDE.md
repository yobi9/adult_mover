# دليل المستخدم — Adult Media Mover

## ما هو التطبيق؟
أداة لسطح مكتب Windows تجمع بين مصدر وجهة بسيطين لمكتبتك. يفحص المجلدات التي
تختارها، ويسأل قاعدة بيانات TMDB عن التصنيف العمري لكل عنوان، ثم **ينقل** فقط
العناوين المصنفة للكبار إلى مجلد وجهة تقرره. لا يُحذف أي ملف من المصدر أبداً.

## 1) المرة الأولى
1. نفّذ `المثبّت` ثم اتبع معالج التثبيت، أو استخدم النسخة **المحمولة** مباشرةً
   (تعمل دون تثبيت، وتحفظ إعداداتها في حساب المستخدم).
2. احصل على مفتاح TMDB API مجاني من <https://www.themoviedb.org/settings/api>.
3. افتح التطبيق وأدخل المفتاح. **خانة «حفظ المفتاح محلياً»** اختيارية صريحة:
   المفتاح يُحفظ على القرص فقط إذا فعّلتها، ويبقى في الذاكرة دون حفظ بخلاف ذلك.

## 2) المصدر والوجهة
- **المصدر (Sources):** أضف مجلداً (أو أكثر) يحتوي مكتبتك. يمكن أن يكون محلياً
  أو على الشبكة (مسار UNC يبدأ بـ `\\`).
- **الوجهة (Destination):** مجلد واحد يصل إليه المحتوى المصنف للكبار.

> سيُطلب منك عدم الاختيار الموجه لحذف — الناقل «يأخذ» المجلد من المصدر ونقله
> بالكامل إلى الوجهة (إعادة تسمية/نقل) دون نسخ أو حذف منفصل.

## 3) «الفحص المتكرر» (Recursive)
- **مفعّل (افتراضي):** النزول داخل مجلدات المصدر بحثاً عن مجلدات محتوى
  (مجلدات تحتوي ملف فيديو مباشرةً: mp4/mkv/avi/mov/wmv/flv/webm).
- **مُطفأ:** يفحص مستوى واحد فقط.
- في الحالتين يُنقل المحتوى النهائي بالكامل بما فيه بنية المجلد، وعند وجود
  بنية سلاسل لا يُفلت مجلد المواسم كمرشّح مستقل.

## 4) تنظيم المكتبة المفضّل
- **فيلم:** مجلد باسم الفيلم، قد يتضمن السنة:
  - `Pulp Fiction (1994)` / `Fight.Club.1999`
- **مسلسل (أفضل نمط):** مجلد جذر + مجلدات مواسم داخله:
  - `Station 19/Season 01`, `الموسم 02`، `S02` …
- **مسلسل (نمط مسطح):** مجلد واحد باسم متسلسل: `Game.of.Thrones.S01`
- أسماء هجينة مثل `الموسم 01` أو `Season 1` أو `S01E01` تدعمها المحلِّل الداخلي.

## 5) ماذا يعني «مكتمل»؟ وما مدلول الألوان؟
- عند انتهاء الوظيفة يظهر شعار **«مكتمل»** في أعلى النافذة مع لوحة إحصاءات
  (تمت معالجتها / نُقلت / تخطيطها / أخطاء).
- **نُقلت:** انتقل المجلد فعلياً إلى الوجهة.
- **تخطيطها:** لم يُنقل (لا يوجد عنوان في TMDB، أو تصنيفه خارج قائمة الهدف،
  أو مصادفة تعارض اسم تم تخطيه بتعقيد آمن). تبقى في المصدر سالمة.
- **أخطاء:** حالات يُرفض فيها النقل مع تسجيل السبب (مثال: العبور بين أقراص
  مختلفة، وجهة غير متاحة, مسار مفقود).

## 6) نقاط أمان مهمة تعمل تلقائياً
- **Fail Closed:** لا نقل إلا بتصنيف TMDB صريح يطابق قائمة الهدف
  (TV-MA, R, NC-17, X, 18+, ADULT, 18) وبنسبة ثقة ≥ 0.55.
- **لا حذف أبداً:** المصدر لا يُقسَّم ولا يُحذف من قبلنا أبداً؛ النقل=تسمية.
- **العبور بين أقراص مختلفة (EXDEV):** يُرفض تلقائياً ولا يُنفَّذ نسخ+حذف;
  السبب يُعرض في السجل.
- **الوجهة لا تُستبدل:** الاسم المتكرر يتحول إلى `Name_1` وتبقى النسخة القديمة.
- **لا تسريب للمفتاح:** المفتاح لا يظهر في السجل أو الملفات المعبّأة.

## 7) الإيقاف أثناء العمل
زر **«إيقاف»** يُنهي المجلد الجاري ثم يقف؛ يمكن إغلاق التطبيق أثناء عمل الوظيفة
عبر تأكيد صريح. إن أُغلق بشكل صارم لسبب ما، تبقى مجلدات المصدر والوجهة
وملفاتها سليمة في أي وقت (النقل ذري لكل مجلد).

## 8) استكشاف الأخطاء
| المشكلة | السبب/الحل |
| --- | --- |
| كل العناوين «تخطيتها» | تحقق من المفتاح واتصال الإنترنت، أو أن التص
فيفات غير المطلوبة |
| العناوين لا تُنقل رغم التصنيف | تأكد أن التصنيف في قائمة الأهداف أعلاه وأن نسبة  الثقة كافية |
| رسالة «مجلدات مختلفة» عند النقل | المصدر والوجهة على أقراص مختلفة؛ لنقل تلقائي ضعهما على نفس
القرص، أو انقل يدوياً |
| لا يظهر شيء في السجل | أعد فتح التطبيق وابدأ الوظيفة؛ ثم أرسل لنا السجل من نافذة السجل |