# دليل المستخدم — Adult Media Mover

## ما هو التطبيق؟
أداة لسطح مكتب Windows تجمع بين مصدر وجهة بسيطين لمكتبتك. يفحص المجلدات التي
تختارها، ويسأل قاعدة بيانات TMDB عن التصنيف العمري لكل عنوان، ثم **ينقل** أو
**ينسخ** العناوين المصنفة للكبار إلى مجلد وجهة تقرره. لا يُحذف أي ملف من المصدر
تلقائياً أبداً.

## 1) المرة الأولى
1. نفّذ `المثبّت` ثم اتبع معالج التثبيت، أو استخدم النسخة **المحمولة** مباشرةً
   (تعمل دون تثبيت، وتحفظ إعداداتها في حساب المستخدم).
2. احصل على مفتاح TMDB API مجاني من <https://www.themoviedb.org/settings/api>.
3. افتح التطبيق وأدخل المفتاح. **خانة «حفظ المفتاح محلياً»** اختيارية صريحة:
   المفتاح يُحفظ على القرص فقط إذا فعّلتها، ويبقى في الذاكرة دون حفظ بخلاف ذلك.

## 2) المصدر والوجهة
- **المصدر (Sources):** أضف مجلداً (أو أكثر) يحتوي مكتبتك. يمكن أن يكون محلياً
  أو على الشبكة (مسار UNC يبدأ بـ `\\`).
- **الوجهة (Destination):** مجلد واحد يصل إليه المحتوى المصنف للكبار.

## 3) «الفحص المتكرر» (Recursive)
- **مفعّل (افتراضي):** النزول داخل مجلدات المصدر بحثاً عن مجلدات محتوى
  (مجلدات تحتوي ملف فيديو مباشرةً: mp4/mkv/avi/mov/wmv/flv/webm).
- **مُطفأ:** يفحص مستوى واحد فقط.
- في الحالتين يُنقل المحتوى النهائي بالكامل بما فيه بنية المجلد، وعند وجود
  بنية سلاسل لا يُفلت مجلد المواسم كمرشّح مستقل.

## 4) سير العمل: فحص ثم قرار ثم تنفيذ
1. **ابدأ الفحص** بالزر «بدء الفحص والتصنيف». هذه المرحلة **لا تمس القرص**:
   يفحص المصادر ويستعلم TMDB ويصنّف كل مجلد فقط.
2. يظهر **مودال نتيجة الفحص**: «تم العثور على X فيلم/مسلسل مصنّف كمحتوى للكبار
   من أصل Y — غير معروف: Z» مع ثلاثة خيارات:
   - **نقل** — يُنقل المحتوى المصنف كاملاً إلى الوجهة.
   - **نسخ** — ينسخ المحتوى إلى الوجهة ويُبقي المصدر كما هو (ويسجّله في تبويب «المنسوخ»).
   - **لاحقاً** — يحفظ العناصر في تبويب «قيد الانتظار» لتُنفَّذ في أي وقت لاحق.
3. كل تنفيذ (نقل/نسخ) يُشغَّل **بعد موافقتك الصريحة** فقط؛ لا شيء يُنقل تلقائياً.

## 5) التبويبات
- **السجل النشط:** كل الأحداث والقرارات والملخصات لحظياً.
- **قيد الانتظار:** العناوين المصنفة للكبار المحفوظة «لاحقاً» — مع أزرار
  نقل/نسخ لكل عنصر أو للكل (نقل الكل / نسخ الكل / مسح الكل).
- **غير المعروف:** مجلدات تعذّر تصنيفها (لا تطابق في TMDB، أو تصنيف غير متوفر،
  أو ثقة منخفضة، أو خطأ استعلام). تبقى في المصدر سالمة ويظهر سبب كل عنصر.
- **المنسوخ:** نسخ نفّذتها سابقاً مع مساراتها — يمكنك حذف النسخة (لا المصدر)
  بزر «حذف النسخة»/«حذف جميع النسخ».

## 6) الفرق بين النقل والنسخ
- **نقل (Move):** نقل فعلي — المصدر يزول من مكانه (الاسم الفريد يُستعمل
  عند تعارض الاسم). يعمل على نفس القرص. بين أقراص مختلفة يُرفض تلقائياً
  برسالة «النقل بين Volumes يتطلب تأكيداً» في السجل.
- **نسخ (Copy):** يبقي المصدر ويُنسخ إلى الوجهة (يعمل بين أقراص مختلفة)، مع
  تحقق من الحجم بعد النسخ، وتسجيل في «المنسوخ».
- في الحالتين: **لا استبدال** — الاسم المتكرر يتحول إلى `Name_1`، والنسخة
  القديمة تبقى.

## 7) ماذا يعني «مكتمل»؟ وما مدلول الشارة؟
- **الشارة** أعلى النافذة: «جاهز» ← «قيد التشغيل» ← «انتهى الفحص — بانتظار
  القرار» ← «مكتمل» أو «متوقف». «متوقف (خطأ حرج)» تعني مشكلة حقيقية (مثل
  مفتاح TMDB مرفوض) — بينما «متوقف» تعني إيقافك اليدوي الآمن.
- لوحة الإحصائيات (تمت معالجتها / نُقلت / تجاوزت / أخطاء) تُحدَّث طوال الوقت.

## 8) نقاط أمان مهمة تعمل تلقائياً
- **Fail Closed:** لا نقل/نسخ إلا بتصنيف TMDB صريح يطابق قائمة الهدف
  (TV-MA, R, NC-17, X, 18+, ADULT, 18) وبنسبة ثقة ≥ 0.55.
- **لا حذف من المصدر تلقائياً أبداً:** النقل=نقل كامل (rename)، والنسخ يبقي
  المصدر؛ حتى حذف النسخ في تبويب «المنسوخ» يتحقق أن المسار داخل الوجهة المعرفة
  قبل الحذف.
- **العبور بين أقراص مختلفة (EXDEV) في النقل:** يُرفض تلقائياً ولا يُنفَّذ
  نسخ+حذف؛ السبب يُعرض في السجل (النسخ المتعمد يعمل بين الأقراص).
- **الوجهة لا تُستبدل:** الاسم المتكرر يتحول إلى `Name_1` وتبقى النسخة القديمة.
- **لا تسريب للمفتاح:** المفتاح لا يظهر في السجل أو الملفات المعبّأة.

## 9) الإيقاف أثناء العمل
زر «إيقاف» يُنهي المجلد الجاري ثم يقف؛ يمكن إغلاق التطبيق أثناء عمل الوظيفة عبر
تأكيد صريح. إن أُغلق بشكل صارم لسبب ما، تبقى مجلدات المصدر والوجهة وملفاتها
سليمة في أي وقت (النقل ذري لكل مجلد).

## 10) استكشاف الأخطاء
| المشكلة | السبب/الحل |
| --- | --- |
| رسالة «أدخل مفتاح TMDB API» | المفتاح فارغ — أدخله من الإعدادات. |
| «متوقف (خطأ حرج)» فور البدء | المفتاح مرفوض (401/403) — تحقق منه أو جدده ثم أعد الفحص. |
| كل العناوين في «غير المعروف» | تحقق من المفتاح واتصال الإنترنت، أو أن العناوين خارج TMDB أو دون تصنيف. |
| العناوين لا تُنقل رغم التصنيف | تأكد أن التصنيف في قائمة الأهداف وأن الثقة كافية (يظهر السبب في السجل). |
| «النقل بين مجلدات مختلفة» عند النقل | المصدر والوجهة على أقراص مختلفة؛ استخدم **نسخ** بدل النقل، أو ضعهما على نفس القرص. |
| «حفظ المفتاح» لا يعمل بعد إعادة التشغيل | فعِّل خانة «حفظ المفتاح محلياً» (الافتراضي: الذاكرة فقط). |
| لا يظهر شيء في السجل | أعد فتح التطبيق وابدأ الفحص؛ ثم أرسل لنا السجل من نافذة السجل. |