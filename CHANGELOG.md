# سجل التغييرات (Changelog)

## 0.2.0 — 2026-09-09 (Scan-First Architecture)

### الجديد
- **Scan-First:** الفحص والتصنيف لا يمس القرص إطلاقاً — يجمع `Known Adult` و `Unknown` فقط، ثم يعرض مودال بثلاثة خيارات: **نقل / نسخ / لاحقاً**.
- **Pending (قيد الانتظار):** حفظ `Known Adult` كحالة "لاحقاً" في `pending.json` (كتابة ذرية) وتنفيذها لاحقاً دون إعادة فحص — تبويب مستقل دائم.
- **Unknown (غير المعروف):** تجميع `not-found/no-rating/low-confidence/tmdb-error` في `unknown.json` للمراجعة — Fail Closed.
- **Copy Mode:** `copyFolder` + `verifyCopy` (مقارنة حجم) مع `uniqueTargetPath`، يبقي المصدر ويُسجل في `copied.json` — تبويب "المنسوخ" مع حذف آمن (`isPathInside`).
- **IPC و UI:** 13 قناة جديدة (`scan:start/summary`, `unknown:*`, `pending:*`, `copied:*`) + واجهة Tabs (السجل/قيد الانتظار/غير المعروف/المنسوخ) + مودالي نتيجة وتأكيد.
- **Pipeline:** فصل `runScanPhase` عن `runExecutePhase` مع الحفاظ على `runScanJob` القديم للتوافق.

### البنية
- 3 مخازن جديدة بنمط `AppConfigStore` (ذري): `unknown-store.ts`, `pending-store.ts`, `copied-store.ts`.
- `JobPhase` أضيف لها `scan-complete` و `copying`.

### الاختبارات
- 153 اختبار وحدة (114 السابقة + 39 جديدة: 7 UnknownStore + 6 PendingStore + 7 CopiedStore + 9 mover-copy + 4 scan-phase + 4 execute-phase + 2 تكامل).

## 0.1.0 — 2026-09-09 (الإصدار الرسمي الأول)

### الإصلاحات
- **D1 — استخراج السنة**: يُستخرج سنة الإنتاج من الأقواس والنتقاط بصيغة
  `Pulp Fiction (1994)` و `Pulp.Fiction.1994` بأنواعها، ويُستخدم كمرشّح بحث
  دقيق مع TMDB (مع رفض التواريخ خارج نطاق معقول).
- **F2 — تدفق السجلات إلى الواجهة**: سجلات العمليات الحية تظهر لحظياً في منطقة
  السجل (`log-area`) عبر قناة IPC جديدة لعناصر تسجيل عابرة، دون تسريب مفتاح API.
- **Bug A — حاويات المواسم (Recursive ON)**: مجلدات مثل `Season 01`, `الموسم 01`,
  `S01`, `S01E01` أصبحت تُصنَّف **حاويات مواسم** داخل جذر سلسلة ولا تُعامل كمرشّح
  مستقل (لم تعد تُستعلم عنها TMDB لوحدها).
- **Bug B — جذور السلاسل (Recursive OFF)**: يُكتشف جذر السلسلة كحاوية أطفالها
  مجلدات مواسم فقط، فيُعالَج كمرشّح واحد ويُنقل كاملاً، وتُحفظ هرمية الملفات؛ مع
  بقاء العمق محفوظاً (المجلدات الغائرة التي لا تحتوي فيديو مباشرةً لا تُرفع).

### الاختبارات
- 114/114 اختبار وحدة ناجح (`npm test`) — بدون إنترنت.
- اختبارات تعبئة حقيقية (Setup / Portable / win-unpacked) مع بحث TMDB حقيقي
  لسيناريوهات: التشغيل، النقل، الإيقاف الآمن، رفض العبور بين الأقراص، دورة
  حياة المفتاح، والإغلاق أثناء العمل.

### إضافة في التحقق من الأمان
- فحص شامل لغياب مفتاح API في جميع ملفات `dist-release` النهائية (0 نتيجة).
- تأكيد عدم تسرب `require/process/ipcRenderer/webFrame/module` إلى الواجهة و
  مطابقة قائمة البيئة (contextBridge) للسماحات المتوقعة تماماً.

### ملاحظات للمعرفة
- إصدار الحزمة هو `0.1.0`؛ الإصدار الوصفي الأول يُطلق عليه «1.0» في التسويق.